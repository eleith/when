import { define } from 'gunshi';

export const caldavSetupCommand = define({
	name: 'caldav',
	description: 'Wizard to setup CalDAV calendar integration',
	run() {
		console.log('Stub: setup caldav calendar');
	}
});
