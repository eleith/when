import { define } from 'gunshi';

export const googleSetupCommand = define({
	name: 'google',
	description: 'Wizard to setup Google Calendar integration',
	run() {
		console.log('Stub: setup google calendar');
	}
});
