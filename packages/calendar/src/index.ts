// @when/calendar — external-calendar I/O for the worker.
// Public API is populated as the calendar module is extracted from apps/web.
export { buildIcs } from './ics.js';
export type { IcsInput, IcsMethod } from './ics.js';
export type { Clock } from './clock.js';
