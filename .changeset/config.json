{
  "$schema": "https://unpkg.com/@changesets/config/schema.json",
  "changelog": "@changesets/cli/changelog",
  "commit": false,
  "fixed": [
    ["@when/cli", "@when/web", "@when/worker"]
  ],
  "linked": [],
  "access": "restricted",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": [
    "@when/calendar",
    "@when/config",
    "@when/db",
    "@when/jobs",
    "@when/video-chat"
  ],
  "privatePackages": {
    "version": true,
    "tag": true
  }
}
